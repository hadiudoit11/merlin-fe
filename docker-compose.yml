# Typequest Full Stack - Docker Compose (Frontend-centric)
# Usage:
#   Full stack dev:    docker compose up
#   Frontend only:     docker compose up frontend
#   Mock API mode:     docker compose --profile mock up frontend-mock
#   Production:        docker compose --profile production up

version: "3.8"

services:
  # ==================== PostgreSQL Database ====================
  db:
    image: postgres:16-alpine
    container_name: typequest-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-typequest}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-typequest_dev}
      POSTGRES_DB: ${POSTGRES_DB:-typequest}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-typequest} -d ${POSTGRES_DB:-typequest}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - typequest-network

  # ==================== Redis (for caching/sessions) ====================
  redis:
    image: redis:7-alpine
    container_name: typequest-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - typequest-network
    profiles:
      - production

  # ==================== FastAPI Backend (Development) ====================
  backend:
    build:
      context: ../Merlin
      dockerfile: Dockerfile
      target: development
    container_name: typequest-backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-typequest}:${POSTGRES_PASSWORD:-typequest_dev}@db:5432/${POSTGRES_DB:-typequest}
      - DEBUG=true
      - CORS_ORIGINS_STR=http://localhost:3000,http://localhost:3001
      - AUTH0_DOMAIN=${AUTH0_DOMAIN:-}
      - AUTH0_API_AUDIENCE=${AUTH0_API_AUDIENCE:-}
      - AUTH0_CLIENT_ID=${AUTH0_CLIENT_ID:-}
      - AUTH0_CLIENT_SECRET=${AUTH0_CLIENT_SECRET:-}
      - SECRET_KEY=${SECRET_KEY:-dev-secret-key-change-in-production}
      - ENCRYPTION_MASTER_KEY=${ENCRYPTION_MASTER_KEY:-}
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ../Merlin:/app
      - /app/__pycache__
      - /app/.git
    networks:
      - typequest-network

  # ==================== FastAPI Backend (Production) ====================
  backend-prod:
    build:
      context: ../Merlin
      dockerfile: Dockerfile
      target: production
    container_name: typequest-backend-prod
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-typequest}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-typequest}
      - DEBUG=false
      - CORS_ORIGINS_STR=${CORS_ORIGINS_STR:-}
      - AUTH0_DOMAIN=${AUTH0_DOMAIN}
      - AUTH0_API_AUDIENCE=${AUTH0_API_AUDIENCE}
      - AUTH0_CLIENT_ID=${AUTH0_CLIENT_ID}
      - AUTH0_CLIENT_SECRET=${AUTH0_CLIENT_SECRET}
      - SECRET_KEY=${SECRET_KEY}
      - ENCRYPTION_MASTER_KEY=${ENCRYPTION_MASTER_KEY}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - typequest-network
    profiles:
      - production

  # ==================== Next.js Frontend (Development) ====================
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: typequest-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://localhost:8000
      - NEXT_PUBLIC_USE_MOCK_API=false
      - AUTH0_CLIENT_ID=${AUTH0_CLIENT_ID:-}
      - AUTH0_CLIENT_SECRET=${AUTH0_CLIENT_SECRET:-}
      - AUTH0_ISSUER=${AUTH0_ISSUER:-}
      - NEXTAUTH_URL=http://localhost:3000
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET:-development-secret}
    depends_on:
      - backend
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    extra_hosts:
      - "host.docker.internal:host-gateway"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - typequest-network

  # ==================== Next.js Frontend (Mock API - No Backend) ====================
  frontend-mock:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: typequest-frontend-mock
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://localhost:8000
      - NEXT_PUBLIC_USE_MOCK_API=true
      - AUTH0_CLIENT_ID=${AUTH0_CLIENT_ID:-}
      - AUTH0_CLIENT_SECRET=${AUTH0_CLIENT_SECRET:-}
      - AUTH0_ISSUER=${AUTH0_ISSUER:-}
      - NEXTAUTH_URL=http://localhost:3000
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET:-development-secret}
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    extra_hosts:
      - "host.docker.internal:host-gateway"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - typequest-network
    profiles:
      - mock

  # ==================== Next.js Frontend (Production) ====================
  frontend-prod:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
      args:
        - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://localhost:8000}
        - NEXT_PUBLIC_USE_MOCK_API=false
    container_name: typequest-frontend-prod
    ports:
      - "3000:3000"
    environment:
      - AUTH0_CLIENT_ID=${AUTH0_CLIENT_ID}
      - AUTH0_CLIENT_SECRET=${AUTH0_CLIENT_SECRET}
      - AUTH0_ISSUER=${AUTH0_ISSUER}
      - NEXTAUTH_URL=${NEXTAUTH_URL}
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
    depends_on:
      - backend-prod
    networks:
      - typequest-network
    profiles:
      - production

  # ==================== Nginx Reverse Proxy (Production) ====================
  nginx:
    image: nginx:alpine
    container_name: typequest-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certs:/etc/nginx/certs:ro
    depends_on:
      - frontend-prod
      - backend-prod
    networks:
      - typequest-network
    profiles:
      - production

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

networks:
  typequest-network:
    driver: bridge

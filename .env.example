# ============================================
# Merlin Frontend Configuration
# ============================================
#
# QUICK START:
#   1. Copy shared env:     cp ../.env.shared .env.local
#   2. Append this file:    cat .env.example >> .env.local
#   3. Start backend:       (see ../Merlin/README.md)
#   4. Run frontend:        npm run dev
#
# Or just copy this file directly:
#   cp .env.example .env.local
#
# ============================================

# ----- Shared Variables (from ../.env.shared) -----
# These should match Merlin backend for consistency:
#   - AUTH0_CLIENT_ID, AUTH0_CLIENT_SECRET, AUTH0_ISSUER
#   - NEXTAUTH_SECRET
#   - BACKEND_URL / NEXT_PUBLIC_BACKEND_URL

# ----- API Mode -----
# 'true' = localStorage mock (for UI development without backend)
# 'false' = real backend (requires Merlin backend running)
NEXT_PUBLIC_USE_MOCK_API=false

# ----- Backend URLs -----
# Local development
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
BACKEND_URL=http://localhost:8000

# Production (uncomment to switch)
# NEXT_PUBLIC_BACKEND_URL=https://merlin-j5sk.onrender.com
# BACKEND_URL=https://merlin-j5sk.onrender.com

# ============================================
# Auth0 Configuration (shared with Merlin backend)
# ============================================
# Must match the backend AUTH0_* values for consistent auth
AUTH0_CLIENT_ID=your-auth0-client-id
AUTH0_CLIENT_SECRET=your-auth0-client-secret
AUTH0_ISSUER=https://your-tenant.auth0.com
AUTH0_AUDIENCE=https://your-tenant.auth0.com/api/v2/

# ============================================
# NextAuth Configuration
# ============================================
# For local dev: http://localhost:3000
# For production: https://your-domain.com
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-change-in-production

# ============================================
# Optional: Google OAuth (via NextAuth)
# ============================================
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ============================================
# Legacy (deprecated - use Auth0 instead)
# ============================================
# Clerk
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Django secret (if using Django backend)
SECRET_KEY=
